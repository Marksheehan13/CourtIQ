export function trend(values=[]){const nums=values.map(Number).filter(Number.isFinite);if(nums.length<2)return {direction:'insufficient',delta:null};const first=nums[0],last=nums[nums.length-1],delta=last-first;return {direction:delta>0?'up':delta<0?'down':'flat',delta,percent:first?delta/Math.abs(first):null}}
export function gameTrend(games=[],teamId,key='pts'){const ordered=[...games].filter(g=>g.teams?.[teamId]).sort((a,b)=>new Date(a.date)-new Date(b.date));return {key,games:ordered.length,trend:trend(ordered.map(g=>g.teams[teamId][key]))}}
export function formSummary(games=[]){const keys=['pts','reb','ast','to'];return Object.fromEntries(keys.map(key=>{const ordered=[...games].filter(g=>g[key]!=null).sort((a,b)=>new Date(b.date)-new Date(a.date));const recent=ordered.slice(0,3).map(g=>Number(g[key])).filter(Number.isFinite);const older=ordered.slice(3,6).map(g=>Number(g[key])).filter(Number.isFinite);const avg=a=>a.length?a.reduce((x,y)=>x+y,0)/a.length:null;const r=avg(recent),o=avg(older);return [key,{key,status:r==null?'insufficient':o==null?'baseline':r>o?'improving':r<o?'declining':'stable',recentAverage:r,olderAverage:o}] }))}
