export function generateInsights(team={},opponent={},sampleSize=0){const out=[];if(team.pts!=null&&opponent.pts!=null)out.push(team.pts>opponent.pts?'Scored more than the opponent in the latest verified game.':'Scored fewer points than the opponent in the latest verified game.');if(team.fga&&team.fgm)out.push(`Field-goal conversion was ${(team.fgm/team.fga*100).toFixed(1)}%.`);if(team.to!=null)out.push(`Turnovers: ${team.to}.`);if(sampleSize>=3)out.push(`Pattern based on ${sampleSize} verified games.`);return out}
