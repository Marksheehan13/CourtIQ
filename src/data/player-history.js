export function buildPlayerHistory(games=[],playerId){const rows=[];for(const g of games){for(const p of g.playerStats||[]){if(String(p.playerId||p.id||'')===String(playerId))rows.push({...p,date:g.date,fixtureId:g.fixtureId})}}rows.sort((a,b)=>new Date(b.date)-new Date(a.date));const avg=k=>{const v=rows.map(r=>Number(r[k])).filter(Number.isFinite);return v.length?v.reduce((a,b)=>a+b,0)/v.length:null};return {playerId,games:rows.length,recent:rows.slice(0,10),averages:{pts:avg('pts'),reb:avg('reb'),ast:avg('ast'),stl:avg('stl'),blk:avg('blk'),to:avg('to')}}}
