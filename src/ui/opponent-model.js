export function buildOpponentModel(games=[],teamId){const verified=games.filter(g=>g.verified!==false&&g.teams?.[teamId]);const avg=k=>{const v=verified.map(g=>Number(g.teams[teamId][k])).filter(Number.isFinite);return v.length?v.reduce((a,b)=>a+b,0)/v.length:null};return {games:verified.length,averages:{pts:avg('pts'),reb:avg('reb'),ast:avg('ast'),to:avg('to')},recent:[...verified].sort((a,b)=>new Date(b.date)-new Date(a.date)).slice(0,5)}}
