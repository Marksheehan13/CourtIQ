export function buildOpponentModel(games=[],teamId){const rows=games.filter(g=>g.teams?.[teamId]);const avg=k=>{const v=rows.map(g=>Number(g.teams[teamId][k])).filter(Number.isFinite);return v.length?v.reduce((a,b)=>a+b,0)/v.length:null};return {teamId,games:rows.length,averages:{pts:avg('pts'),reb:avg('reb'),ast:avg('ast'),to:avg('to')},recent:rows.slice().sort((a,b)=>new Date(b.date)-new Date(a.date)).slice(0,5)}}
