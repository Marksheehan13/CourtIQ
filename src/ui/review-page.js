const esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
export function reviewPageHtml({imageData,fields=[],title='OCR Review'}={}){return `<section class="ocr-review"><header><span class="eyebrow">DATA VERIFICATION</span><h1>${esc(title)}</h1><p>Every extracted value should be checked before the game becomes trusted data.</p></header><div class="review-layout"><div class="review-image"><img src="${imageData||''}" alt="Source screenshot"></div><div class="review-fields">${fields.map(f=>`<label><span>${esc(f.label)} ${f.confidence!=null?`· ${Math.round(f.confidence*100)}%`:''}</span><input data-field-id="${esc(f.id)}" value="${esc(f.value)}"></label>`).join('')}<button class="primary" id="save-review">Save review</button></div></div></section>`}
