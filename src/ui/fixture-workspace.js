export function fixtureWorkspaceHtml(fixture,{screenshots=[]}={}){
 const esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
 return `<section class="fixture-workspace"><div class="workspace-head"><div><span class="eyebrow">SCOUTING SESSION</span><h2>${esc(fixture.home)} vs ${esc(fixture.away)}</h2><p>${esc(fixture.date)} · ${esc(fixture.venue||'Venue TBC')}</p></div><span class="status-pill">${esc(fixture.status||'scouting')}</span></div><div class="workflow"><div class="step active">01<span>Screenshots</span></div><div class="step">02<span>OCR review</span></div><div class="step">03<span>Verification</span></div><div class="step">04<span>Analytics</span></div><div class="step">05<span>Report</span></div></div><div class="upload-card"><h3>Add Swish screenshots</h3><p>Upload as many screenshots as this fixture needs. They remain isolated to this scouting session.</p><label class="dropzone"><input id="fixture-files" type="file" accept="image/*" multiple><strong>Choose screenshots</strong><span>PNG, JPG or WEBP · multiple files supported</span></label></div><div class="screenshot-grid">${screenshots.map(s=>`<article class="screenshot-card"><img src="${s.data}" alt="${esc(s.name)}"><div><strong>${esc(s.name)}</strong><span>Unreviewed</span></div></article>`).join('')}</div></section>`;
}
