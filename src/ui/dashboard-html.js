export function dashboardHtml({counts,fixtures}){const esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));const cards=[['Games',counts.total],['In progress',counts.inProgress],['Verified',counts.verified],['Reports',counts.reported]].map(([l,v])=>`<div class="stat-card"><span>${l}</span><strong>${v}</strong></div>`).join('');const rows=fixtures.map(f=>`<tr data-fixture-id="${esc(f.id)}"><td><strong>${esc(f.home)} vs ${esc(f.away)}</strong><small>${esc(f.date)} · ${esc(f.venue||'Venue TBC')}</small></td><td>${esc(f.stageLabel)}</td><td>${f.screenshotCount}</td><td><button data-open-fixture="${esc(f.id)}">Open</button></td></tr>`).join('');return `<section class="dashboard"><div class="dashboard-head"><div><span class="eyebrow">COURTIQ</span><h1>Scouting dashboard</h1><p>Track every fixture from screenshots to verified intelligence.</p></div></div><div class="stat-grid">${cards}</div><div class="fixture-list"><div class="list-head"><h2>Fixtures</h2><input id="fixture-search" placeholder="Search teams or venues"></div><table><thead><tr><th>Fixture</th><th>Status</th><th>Screenshots</th><th></th></tr></thead><tbody>${rows||'<tr><td colspan="4">No fixtures yet.</td></tr>'}</tbody></table></div></section>`}
