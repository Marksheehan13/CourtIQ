const esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));
export function createReviewModel({fixtureId,screenshotId,imageData,candidates={}}){return {fixtureId,screenshotId,imageData,candidates,status:'needs_review',createdAt:new Date().toISOString()}}
export function reviewPanelHtml(review){const rows=Object.entries(review.candidates||{}).map(([k,v])=>`<label class="field"><span>${esc(k)}</span><input data-field="${esc(k)}" value="${esc(v)}"></label>`).join('');return `<div class="review-panel"><div class="review-image">${review.imageData?`<img src="${review.imageData}" alt="Source screenshot">`:'<div class="empty">No image</div>'}</div><div class="review-fields"><h3>OCR review</h3>${rows||'<p class="muted">No OCR candidate values yet. Connect an OCR provider or enter verified values manually.</p>'}<button class="primary" id="verify-data">Verify values</button></div></div>`}
