export function createHttpOcrProvider({endpoint,headers={}}={}){if(!endpoint)throw new Error('OCR endpoint is required');return {name:'http',async extract({image,fixtureId,screenshotId}){const res=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json',...headers},body:JSON.stringify({image,fixtureId,screenshotId})});if(!res.ok)throw new Error(`OCR request failed: ${res.status}`);return res.json()}}}
