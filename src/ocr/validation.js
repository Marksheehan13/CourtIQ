const NUMERIC_FIELDS=['min','pts','fgm','fga','tpm','tpa','ftm','fta','oreb','dreb','reb','ast','stl','blk','to','pf','plusMinus'];
export function validatePlayerRow(row={}){const errors=[];const warnings=[];for(const key of NUMERIC_FIELDS){if(row[key]!==null&&row[key]!==undefined&&row[key]!==''&&!Number.isFinite(Number(row[key])))errors.push(`${key} must be numeric`)}const n=k=>Number(row[k])||0;if(n('fgm')>n('fga'))errors.push('FGM cannot exceed FGA');if(n('tpm')>n('tpa'))errors.push('3PM cannot exceed 3PA');if(n('ftm')>n('fta'))errors.push('FTM cannot exceed FTA');if(n('oreb')+n('dreb')!==n('reb')&&row.reb!=null)warnings.push('Rebounds do not equal offensive + defensive rebounds');if(n('tpm')>n('fgm'))warnings.push('3PM cannot normally exceed FGM');return {valid:errors.length===0,errors,warnings}}
export function validateTeamTotals(team={}){const errors=[];const warnings=[];const n=k=>Number(team[k])||0;if(n('fgm')>n('fga'))errors.push('FGM cannot exceed FGA');if(n('tpm')>n('tpa'))errors.push('3PM cannot exceed 3PA');if(n('ftm')>n('fta'))errors.push('FTM cannot exceed FTA');if(team.reb!=null&&team.oreb!=null&&team.dreb!=null&&n('oreb')+n('dreb')!==n('reb'))warnings.push('Team rebound components do not reconcile');return {valid:errors.length===0,errors,warnings}}
export function validateExtraction(result={}){const rows=result.rows||[];const rowChecks=rows.map((r,i)=>({index:i,...validatePlayerRow(r)}));const team=validateTeamTotals(result.teamTotals||{});return {valid:team.valid&&rowChecks.every(x=>x.valid),team,rowChecks,hardErrors:rowChecks.flatMap(x=>x.errors.map(e=>`Player ${x.index+1}: ${e}`)).concat(team.errors),warnings:rowChecks.flatMap(x=>x.warnings.map(e=>`Player ${x.index+1}: ${e}`)).concat(team.warnings)}}
