const prompt=`You are CourtIQ's official basketball game-report extraction engine. Read this PDF game report and return ONLY valid JSON. Extract final score, quarter scores, team totals and player statistics when visible. JSON shape: {homeScore,awayScore,quarterScores:[{home,away}],teamTotals:{home:{},away:{}},rows:[{number,name,min,pts,fgm,fga,tpm,tpa,ftm,fta,oreb,dreb,reb,ast,stl,blk,to,pf,plusMinus}],notes}. Preserve uncertainty; never invent values.`;
const json=(s,b)=>new Response(JSON.stringify(b),{status:s,headers:{'content-type':'application/json'}});
function dataPart(v){const m=String(v||'').match(/^data:(.*?);base64,(.*)$/);if(!m)throw new Error('Invalid PDF');return {mimeType:m[1],data:m[2]}}
export default async request=>{if(request.method!=='POST')return json(405,{error:'POST only'});try{const {pdf}=await request.json();const p=dataPart(pdf);const key=process.env.GEMINI_API_KEY;if(!key)throw new Error('GEMINI_API_KEY is not configured');const model=process.env.GEMINI_MODEL||'gemini-2.5-flash';const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent?key=${encodeURIComponent(key)}`,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({contents:[{parts:[{text:prompt},{inlineData:p}]}],generationConfig:{temperature:0,responseMimeType:'application/json'}})});if(!r.ok)throw new Error(`Gemini ${r.status}: ${await r.text()}`);const x=await r.json();const text=x.candidates?.[0]?.content?.parts?.map(a=>a.text||'').join('')||'{}';return json(200,{provider:'gemini',result:JSON.parse(text.replace(/^```json\s*/,'').replace(/\s*```$/,''))})}catch(e){return json(400,{error:e.message})}};
